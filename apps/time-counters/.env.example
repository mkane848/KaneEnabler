# Optional — powers the shared sign-in menu (same account as the platform
# home page and commander recommender). Leave both blank to run without
# sign-in; the tracker itself needs neither. Get these from your Supabase
# project's API settings.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
