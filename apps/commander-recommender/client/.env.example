# Leave blank for local dev (Vite proxies /api to http://localhost:4000, see vite.config.ts).
# In production, set this to your deployed backend's URL, e.g.:
# VITE_API_URL=https://mtg-recommender-server.onrender.com
VITE_API_URL=
