# Optional — powers the shared sign-in menu (same account as the commander
# recommender). Leave both blank to run without sign-in; the tool cards work
# either way. Get these from your Supabase project's API settings.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
