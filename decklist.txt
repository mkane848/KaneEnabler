# Add the exact card names from your Commander deck below, one per line.
# Lines starting with # are ignored, as are blank lines. Matching against
# Scryfall is case-insensitive but otherwise exact, so spelling matters.
#
# Once your list is ready, run:
#   npm run fetch-cards
#
# That pulls Scryfall's Oracle Cards bulk file and filters it down to just
# these cards, saving the result to src/data/cards.json. Every result is
# also filtered to this deck's Jeskai (White/Blue/Red) color identity —
# see scripts/fetch-card-data.mjs.

# Commanders
The Tenth Doctor
Rose Tyler

# Add the rest of your 99 below.
